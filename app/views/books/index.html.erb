<%= render 'users/list', user: @user, book: @book_new = Book.new %>

<table>
<% @books.each do |book| %>
       <th>title</th>
        <th>opinion</th>

    <tr>
        <td><%=  link_to book.title, book_path(book.id) %></td>
    </tr>
    <tr>
        <td><%= book.body %></td>
    </tr>
<% end %>

