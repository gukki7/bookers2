<body>
<p>
  <strong>title:</strong> <%= @book.title %>
  </p>
<p>
  <strong>Book:</strong> <%= @book.body %>
  </p>
<p>
  印象を共有し交換することができます
  </p>
  <%=  link_to "Edit", edit_book_path(@book.id) %>
  <%=  link_to "back", books_path %>
</body>

  <% if @book.user == current_user %>
    <%= link_to "削除", book_path(@book), method: :delete %>
  <% end %>